目前正在看大佬总结的minimind相关的文件，下面我来整理一下这些知识点(里面的文字全部来自于from-minimind-to-more
大佬写的项目)
一.分词（Tokenization）
分词器（Tokenizer）将连续的文本流转化为离散的数字序列（Token IDs），使得计算机能够进行计算、统计和理解。
分词虽然是字符串预处理步骤，但是里面包含的算法等等会影响到模型的性能等


二、文本表示演变
2.1 词级别（Word-level）分词
将 "I love AI." 切分为 ["I", "love", "AI", "."]
单词太多（形式啥的），词表爆炸
未登录词导致任务失败

2.2 字符级别（Character-level）分词
将 "love" 切分为 ["l", "o", "v", "e"]
消除了未登录词的问题
序列过长-计算成本很高
语义缺失-模型需要花费大量的层数和参数去组合字符以识别出“词”的概念，这浪费了模型的表达能力

2.3 子词（Subword）分词
常用词保持完整，罕见词拆分为有意义的子部件（Subword units），tokenization” 在子词分词器中可能被拆分为 “token” 和 “ization”
模型既能高效处理常见词，又能通过组合词根和词缀来泛化理解未见过的复合词（如 “modernization”, “optimization”）
当前三种子词算法：
BPE (Byte-Pair Encoding)：基于频率的合并策略，广泛用于GPT系列、Llama、RoBERTa
WordPiece：基于概率（似然度）的合并策略，起源于BERT
Unigram：基于概率的剪枝策略（从大词表删减），用于SentencePiece (ALBert, T5)


三、BPE (Byte-Pair Encoding)
BPE的训练过程本质上是一个迭代的数据压缩过程
1.初始化：将所有文本拆解为基础单元（通常是字节）
2.统计：统计所有相邻单元对（Pair）在数据中出现的频率
3.合并：找到频率最高的那个对（例如 ('e', 's')），将其合并为一个新的符号（'es'），并分配一个新的ID
4.迭代：重复步骤2和3，直到达到预设的词表大小（Vocabulary Size）

3.1统计频率代码详解


